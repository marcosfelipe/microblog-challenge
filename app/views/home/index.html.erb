<!-- <%= user_signed_in? %>

<% if user_signed_in? %>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
<% else %>
  <%= link_to 'sign in', new_user_session_path %>
<% end %> -->

<home username="<%= current_user.username if user_signed_in? %>"></home>
